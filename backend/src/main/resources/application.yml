server:
  port: ${PORT:8080}

spring:
  application:
    name: ecoregistru
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/ecoregistru}
    driver-class-name: org.postgresql.Driver
    username: ${DATABASE_USERNAME:eco}
    password: ${DATABASE_PASSWORD:eco}
  jpa:
    hibernate:
      # Schema is owned by Flyway. NEVER 'update' (quality bar).
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB
  mail:
    protocol: smtp
    port: ${MAIL_PORT:587}
    host: ${MAIL_HOST:smtp.zoho.eu}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
        starttls:
          enabled: true

app:
  # Base URL used in email links (verification, reset).
  frontend-base-url: ${FRONTEND_BASE_URL:http://localhost:5173}
  mail:
    from: ${MAIL_FROM:contact@ecoregistru.ro}
  jwt:
    # Base64-encoded HMAC secret. MUST be set in every environment.
    secret: ${JWT_SECRET:}
  cloudinary:
    # Single URL form: cloudinary://<api_key>:<api_secret>@<cloud_name>
    url: ${CLOUDINARY_URL:}
    folder: ${CLOUDINARY_FOLDER:ecoregistru}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    ro.ecoregistru: INFO
